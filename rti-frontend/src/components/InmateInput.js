//I don't think I'll use this if the API works
//class component because it's a form
//can have local state that controls values or 
//have form data available in redux store

import React, {Component} from 'react'

class InmateInput extends Component {
    render(){
        return (
            <div>
                InmateInput
            </div>
        )
    }
}

export default InmateInput